<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrsoftit.vioplus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VioPlus">
        <activity android:name=".activity.OverlyActivity"></activity>
        <activity android:name=".activity.Test1Activity"></activity>
        <activity android:name=".playstore.AppInfoActivity" />
        <activity android:name=".facebookfragment.FbPageInfoActivity" />
        <activity android:name=".youtubefragment.YoutubeVideInfoActivity"></activity>
        <activity android:name=".activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TestActivity" />
        <activity android:name=".activity.YoutubeActivity" />
        <activity android:name=".activity.MainActivity" />

        <service
            android:name=".service.MainService"
            android:exported="false" />
    </application>

</manifest>